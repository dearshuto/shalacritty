[package]
name = "term-gfx"
version = "0.1.0"
edition = "2021"

[dependencies]
crossfont = { version = "0.8.0" }
naga = { version = "23.1.0", features = ["spv-in"] }

[dev-dependencies]
bmp = "*"
naga = { version = "23.1.0", features = ["glsl-in", "spv-out"] }
